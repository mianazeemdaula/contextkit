/** @type {import('next').NextConfig} */
// Next 15 app router config — https://nextjs.org/docs/app/getting-started/installation
const nextConfig = {
  reactStrictMode: true
};

export default nextConfig;
