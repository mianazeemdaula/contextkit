# Supabase project settings — required for auth + persistence routes.
# Marketing pages render fine without these (lib/env.ts allows missing in dev).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE=
